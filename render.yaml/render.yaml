services:
  - type: web
    name: pdf-bee
    env: python

    buildCommand: |
      apt-get update
      apt-get install -y poppler-utils
      pip install -r requirements.txt

    startCommand: gunicorn app:app